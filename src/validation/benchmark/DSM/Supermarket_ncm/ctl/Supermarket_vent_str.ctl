assumes air based heating and cooling in shop offices cafe storage
* Building
Specific controls for UK NCM activities
  10  # No. of functions
* Control function    1
# senses a mix of db T and MRT in the current zone.
   -2    0   50    0  # sensor data
# actuates air point of the current zone
    0    0    0  # actuator data
    1  # all daytypes
    1  365  # valid Tue-01-Jan - Wed-31-Dec
     1  # No. of periods in day: weekdays    
    0   23   0.000  # ctl type, law (Sensed temp setpts), start @
      2.  # No. of data items
  5.000 6.000
* Control function    2
# senses a mix of db T and MRT in the current zone.
   -2    0   50    0  # sensor data
# actuates air point of the current zone
    0    0    0  # actuator data
    1  # all daytypes
    1  365  # valid Tue-01-Jan - Wed-31-Dec
     1  # No. of periods in day: weekdays    
    0   23   0.000  # ctl type, law (Sensed temp setpts), start @
      2.  # No. of data items
  10.000 11.000
* Control function    3
# senses a mix of db T and MRT in the current zone.
   -2    0   50    0  # sensor data
# actuates air point of the current zone
    0    0    0  # actuator data
    1  # all daytypes
    1  365  # valid Tue-01-Jan - Wed-31-Dec
     1  # No. of periods in day: weekdays    
    0   23   0.000  # ctl type, law (Sensed temp setpts), start @
      2.  # No. of data items
  15.000 16.000
* Control function    4
# senses a mix of db T and MRT in the current zone.
   -2    0   50    0  # sensor data
# actuates air point of the current zone
    0    0    0  # actuator data
    1  # all daytypes
    1  365  # valid Tue-01-Jan - Wed-31-Dec
     1  # No. of periods in day: weekdays    
    0   23   0.000  # ctl type, law (Sensed temp setpts), start @
      2.  # No. of data items
  20.000 21.000
* Control function    5
# senses a mix of db T and MRT in the current zone.
   -2    0   50    0  # sensor data
# actuates air point of the current zone
    0    0    0  # actuator data
    1  # all daytypes
    1  365  # valid Tue-01-Jan - Wed-31-Dec
     1  # No. of periods in day: weekdays    
    0   23   0.000  # ctl type, law (Sensed temp setpts), start @
      2.  # No. of data items
  25.000 26.000
* Control function    6
# senses a mix of db T and MRT in the current zone.
   -2    0   50    0  # sensor data
# actuates air point of the current zone
    0    0    0  # actuator data
    1  # all daytypes
    1  365  # valid Tue-01-Jan - Wed-31-Dec
     1  # No. of periods in day: weekdays    
    0   23   0.000  # ctl type, law (Sensed temp setpts), start @
      2.  # No. of data items
  30.000 31.000
* Control function    7
# senses a mix of db T and MRT in the current zone.
   -2    0   50    0  # sensor data
# actuates air point of the current zone
    0    0    0  # actuator data
    1  # all daytypes
    1  365  # valid Tue-01-Jan - Wed-31-Dec
     1  # No. of periods in day: weekdays    
    0   23   0.000  # ctl type, law (Sensed temp setpts), start @
      2.  # No. of data items
  35.000 36.000
* Control function    8
# senses a mix of db T and MRT in the current zone.
   -2    0   50    0  # sensor data
# actuates air point of the current zone
    0    0    0  # actuator data
    1  # all daytypes
    1  365  # valid Tue-01-Jan - Wed-31-Dec
     1  # No. of periods in day: weekdays    
    0   23   0.000  # ctl type, law (Sensed temp setpts), start @
      2.  # No. of data items
  40.000 41.000
* Control function    9
# senses a mix of db T and MRT in the current zone.
   -2    0   50    0  # sensor data
# actuates air point of the current zone
    0    0    0  # actuator data
    1  # all daytypes
    1  365  # valid Tue-01-Jan - Wed-31-Dec
     1  # No. of periods in day: weekdays    
    0   23   0.000  # ctl type, law (Sensed temp setpts), start @
      2.  # No. of data items
  45.000 46.000
* Control function   10
# senses a mix of db T and MRT in the current zone.
   -2    0   50    0  # sensor data
# actuates air point of the current zone
    0    0    0  # actuator data
    1  # all daytypes
    1  365  # valid Tue-01-Jan - Wed-31-Dec
     1  # No. of periods in day: weekdays    
    0   23   0.000  # ctl type, law (Sensed temp setpts), start @
      2.  # No. of data items
  50.000 51.000
# Function:Zone links
 5,6,3,1,4,1,3,9,2,2,7,10,10,8
